syntax = "proto3";
package hello;

service Say {
  rpc Send (SayRequest) returns (SayResponse);
  rpc SendStream(SayRequest) returns (stream SayResponse);
}

message SayRequest {
  string name = 1;
}

message SayResponse {
  string message = 1;
}
